spring:
  datasource:
    url: ${DATABASE_URL}
    username: ${DATABASE_USERNAME}
    password: ${DATABASE_PASSWORD}
  data:
    redis:
      url: ${REDIS_URL}
  cache:
    type: redis
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

